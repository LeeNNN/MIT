用html语言写一个UI前端框架，server我自己写。
界面1：对多个研究的结果进行管理（增 删 改 查等操作），
包含的字段有：作者，期刊，年，卷期页，样本量，提取的参数（放到5个字段），平均年龄，男性比例，质量等级，估计参数1，估计参数2，。。。
重要子界面1.1：一个弹出页面，根据提取的参数，选择方法，后台函数估计出来的参数保存到表里相应的字段（函数我们提供）
重要子界面1.2：每一篇文献的质量等级评分（弹出页面，评分项目可以用另外一个表来管理，评分的结果求和记录到主表）
 
要求，字段名称可编辑，可加可减，文献可拖动调整顺序，后台如果能用数据库，就更好。提供pubmed的地址，可以自动添加必要的文献信息
==这一部分类似于把文献管理起来。
 
 
第二部分：meta分析：
常规的meta分析出图，漏斗图，森林图，以及结果的自动化文字描述（写好模板，填空即可）。
要求：允许从文献列表中勾选哪些文献纳入分析，同时允许定义subgroup
 
第三部分，bayesian meta（程序我们提供，只要展示结果和图片）
 
第四部分，meta回归，后台程序我们提供，前端需要：选择哪些研究纳入，哪些变量作为x，画图时候x 和y的范围，传到后台画图。前端展示